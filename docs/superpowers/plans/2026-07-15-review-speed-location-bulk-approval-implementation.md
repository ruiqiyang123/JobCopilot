# JobCopilot 城市识别、快速 AI 筛选与批量批准实现计划

日期：2026-07-15  
规格：`docs/superpowers/specs/2026-07-15-review-speed-location-bulk-approval-design.md`

状态：✅ 已完成。

## ✅ 任务 1：可信位置解析

- 先在 `tests/job-filters.test.js` 和 `tests/job-detail.test.js` 增加标题/JD 污染、明确异地、搜索城市兜底和旧字段清理测试。
- 在 `src/job-filters.js` 增加独立位置事实解析、来源规范化和搜索城市兜底。
- 在 `src/content-search.js` 与 `src/selectors.js` 提供独立位置 DOM 文本，不把完整 JD 当作位置事实。
- 在 `src/job-detail.js` 保存 `citySource` 与位置解析版本。
- 在 `src/background.js` 的硬筛选入口统一应用位置兜底和旧数据迁移。

## ✅ 任务 2：两级 AI 筛选

- 新增快速筛选结果校验模块和单元测试，支持纯 JSON、代码块 JSON、非法结果降级。
- 将自动 `screenJob` 改为短结构快速筛选；保留原六维评分为独立的按需调用。
- 添加快速筛选状态、错误和详细评分状态的持久化字段。
- 增加后台消息：重试快速判断、生成详细评分。

## ✅ 任务 3：异步人工确认

- “确认信息符合”先持久化人工确认和 `running` 状态，再启动后台快速判断。
- 侧边栏立即显示绿色已确认与“AI 快速判断中…”，允许继续操作其他岗位。
- 成功后自动刷新分类；失败或恢复陈旧任务时显示可重试状态。
- 增加消息链路和恢复测试。

## ✅ 任务 4：批量批准

- 推荐岗位卡片增加选择框，推荐分类增加全选和“一键批准推荐岗位（N）”。
- 后台批量接口重新校验岗位资格，一次更新、一次持久化、一次通知。
- 增加选择状态、批量资格、批量持久化和“不触发预演/投递”测试。

## ✅ 任务 5：集成与交付

- 更新清单版本与 README。
- 运行全部 Node 测试、脚本语法检查、manifest JSON 检查、密钥扫描和 `git diff --check`。
- 提交代码，推送当前分支并更新现有 PR。

验收记录：Node 测试 143/143 通过；侧边栏窄屏原型完成视觉检查，无控制台错误；未访问 BOSS、未执行真实投递。
