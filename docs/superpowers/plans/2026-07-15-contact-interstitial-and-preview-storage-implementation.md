# JobCopilot 建联拦截与预演存储去重实施计划

日期：2026-07-15

关联规格：`docs/superpowers/specs/2026-07-15-contact-interstitial-and-preview-storage-design.md`

## 任务 1：订阅弹窗安全处理

- [x] 增加可独立测试的订阅弹窗识别模块，只匹配 BOSS 订阅回复文案。
- [x] 在岗位页脚本中接入“识别 → 唯一关闭 → 等待消失 → 重新定位 → 只重试一次”流程。
- [x] 将新模块加入 Manifest 内容脚本和后台动态注入顺序。
- [x] 覆盖正常跳转、匹配弹窗、非目标弹窗、关闭按钮不唯一和重复弹窗测试。

## 任务 2：聊天岗位身份宽限

- [x] 聊天观察器为直接关联且暂缺 `jobId` 的标签维护 5 秒待定状态。
- [x] 在宽限期内补齐正确岗位 ID 时继续，错误 ID 或宽限超时时停止。
- [x] 保持总建联超时、批次中止和监听器清理行为不变。
- [x] 覆盖正确补齐、错误补齐、持续缺失、无关标签和清理测试。

## 任务 3：预演图片单份存储

- [x] 新预演只保存 `resumeImageFingerprint`，不再保存 Base64 图片正文。
- [x] 正式投递从已校验招呼方案读取图片，并再次核对图片指纹。
- [x] 迁移旧预演时保留开场与确认状态、补齐指纹并删除嵌入图片。
- [x] 迁移完成后删除旧顶层图片副本，保证迁移幂等。
- [x] 覆盖新预演、正式发送校验、旧数据迁移、缺失指纹和重复迁移测试。

## 任务 4：存储错误与回归

- [x] 增加存储配额错误识别，将 Chromium 原始错误转换为中文提示。
- [x] 更新集成测试，确保身份确认前不发送、预演不复制图片。
- [x] 更新版本号和 README 故障修复说明。
- [x] 运行全部 Node 测试、JavaScript 语法、Manifest、差异和 API Key 检查。
- [x] 提交并推送当前分支到现有 PR #2；不执行真实投递。
