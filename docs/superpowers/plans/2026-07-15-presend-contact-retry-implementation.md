# JobCopilot 发送前建联自动重试实施计划

日期：2026-07-15

关联规格：`docs/superpowers/specs/2026-07-15-presend-contact-retry-design.md`

## 任务 1：可测试的重试边界

- [x] 新增纯规则模块，只允许第一次 `missing_job_id` 在发送前重试。
- [x] 明确拒绝岗位不一致、其他建联错误、暂停、中止、第二次失败和已经开始发送的情况。
- [x] 为所有允许与拒绝分支增加单元测试。

## 任务 2：后台投递编排

- [x] 将聊天岗位 ID 宽限从 5 秒调整为 12 秒。
- [x] 抽取“打开详情 → 身份/资格/预演复核 → 建立聊天页”步骤，供两次建联尝试复用。
- [x] 第一次 `missing_job_id` 时记录日志并重新打开岗位详情。
- [x] 每次尝试都重新执行发送前完整校验，成功后只调用一次 `SEND_BUNDLE`。
- [x] 保持 `executedIds` 单次记录和临时标签页统一去重清理。

## 任务 3：回归与发布

- [x] 更新集成测试，确认自动重试发生在 `SEND_BUNDLE` 之前且发送失败不重试。
- [x] 更新版本号和 README 使用说明。
- [x] 运行全部测试、JavaScript 语法、Manifest、差异和 API Key 检查。
- [ ] 提交并推送当前分支到现有 PR #2；不执行真实投递。
